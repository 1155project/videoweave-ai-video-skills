{
  "_note": "This file is for Claude Code CLI (HTTP transport). Claude Desktop users need videoweave-mcp-bridge instead — see README.md.",
  "mcpServers": {
    "videoweave": {
      "url": "https://api.videoweave.io/mcp/v1",
      "headers": {
        "Authorization": "Bearer vw_YOUR_API_KEY_HERE"
      }
    }
  }
}
